# 2026-08-16 — CLAUDE.md

## Authored CLAUDE.md

Analyzed the repo and wrote `CLAUDE.md` covering:

- **Build**: `LinkPiler.xcodeproj` is the live project (`LinkCollector.xcodeproj` is a stub without
  `project.pbxproj`); verified `xcodebuild` commands for macOS and the iOS simulator; noted that
  there are no test targets and no lint config.
- **Layout**: the four targets and their source directories, the LinkPiler/LinkCollector naming
  split, the unreferenced leftover directories, and the fact that cross-target code sharing is done
  through target membership rather than a shared framework.
- **Architecture**: AppDelegate-owned `LinkCollectorViewModel` injected as an `@EnvironmentObject`
  (no `@FetchRequest`); the `Persistence` → `PersistenceHelper` → view model stack; search running
  through Spotlight `CSSearchQuery` and mapping objectID URIs back to entities; the three different
  ways the app, share extensions, and widget reach the same data; inline `#if canImport(UIKit)`
  platform branching; strict concurrency on Swift 5 language mode.
- **Conventions**: commit prefixes, version-branch → PR → `main` flow, `os.Logger` with privacy
  annotations, `viewModel.message` for user-facing errors.
- Pointers to `2026.md` as the 2.0 plan and a note that `README.md` is stale.

No Cursor, Copilot, Codex, or Gemini configs were present to import.

## Added the work log instruction

Added the `## Work log` section to `CLAUDE.md` directing that a summary be appended to
`docs/worklog/YYYY-MM-DD-<topic>.md` after each step or major task.

## Committed

`27bae8a docs: Add CLAUDE.md` on branch `2026`. Only `CLAUDE.md` was included.
